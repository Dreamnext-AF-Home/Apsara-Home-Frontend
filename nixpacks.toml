[phases.setup]
nixPkgs = [
  "nodejs_20"
]

[phases.install]
cmds = [
  "npm exec --yes pnpm@11.0.9 -- install --frozen-lockfile"
]

[phases.build]
cmds = [
  "npm exec --yes pnpm@11.0.9 -- run build"
]

[start]
cmd = "npm exec --yes pnpm@11.0.9 -- run start"
